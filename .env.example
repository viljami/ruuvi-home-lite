# Environment Configuration Template
# Copy this file to .env and update with your actual values
# DO NOT commit .env file to version control

# MQTT Broker Configuration
MQTT_HOST=localhost
MQTT_PORT=8883
MQTT_USER=ruuvi
MQTT_PASS=GENERATED_DURING_SETUP

# Server Configuration
NODE_ENV=production
SERVER_IP=DETECTED_DURING_SETUP

# Optional: Custom database path
# DB_PATH=data/ruuvi.db

# Optional: Custom server port
# SERVER_PORT=3000

# Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Run setup script: ./setup.sh
# 3. Setup script will generate secure MQTT_PASS automatically
# 4. Start application: make start
